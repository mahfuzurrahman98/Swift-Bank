export enum UserRole {
    GUEST = "guest",
    ADMIN = "admin",
    ACCOUNT_HOLDER = "account-holder",
}

export enum UserStatus {
    PENDING = "pending",
    ACTIVE = "active",
    INACTIVE = "inactive",
    SUSPENDED = "suspended",
}
