export enum InvalidateByType {
    NEW_REQUEST = "new-request",
    MANUAL = "manual",
    EXPIRED = "expired",
}

export enum DeviceType {
    MOBILE = "mobile",
    DESKTOP = "desktop",
    TABLET = "tablet",
}
