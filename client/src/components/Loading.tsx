const Loading = () => {
    return (
        <div className="flex flex-col items-center justify-center h-screen">
            <div className="w-24 h-24 border-8 border-dashed rounded-full animate-spin border-amber-800"></div>
        </div>
    );
};

export default Loading;
